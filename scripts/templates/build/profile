# The MACHINE environment variable can be used to pick a
# machine, look in meta-ccc/conf/machine for a list of values.

export MACHINE=eis-ccc

export BB_ENV_EXTRAWHITE=MACHINE
TOPDIR=`dirname ${PWD}`
export PATH=${TOPDIR}/oe-core/scripts:${TOPDIR}/bitbake/bin:$PATH
